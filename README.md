Make sure `pwd` is in your `PYTHONPATH` then run `make`.
Congratulations !

### Note
It will try to output the VHDL files somewhere in `../vivado/something`. Check the variable `IP_REPO` in the `Makefile` to change that.
